const UI_CONFIG = {
    borderCanvas: 20,
    fieldHeight: canvasHeight + 5,
    fieldWidth: canvasWidth + 5,
    fieldColor: "#F7D800",
    fieldBorderColor: "#171717",
    snakeColor: "#3BD80D"
  };
  const GAME_CONFIG = {
    snakeSpeed: 65,
    snakePartSize: 10,
    snakeLength: 5
  };